version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: latest
    commands:
      - pwd
  build:
    commands:
      - printenv > .env

artifacts:
  files:
    - '**/*'
  discard-paths: no
